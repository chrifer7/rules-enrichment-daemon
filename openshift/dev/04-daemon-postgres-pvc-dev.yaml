apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: daemon-postgres-dev-data
  labels:
    app.kubernetes.io/name: daemon-postgres
    app.kubernetes.io/environment: dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
