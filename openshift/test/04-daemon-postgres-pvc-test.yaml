apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: daemon-postgres-test-data
  labels:
    app.kubernetes.io/name: daemon-postgres
    app.kubernetes.io/environment: test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
